# Release workflow

- Any future release must include release notes that summarize the shipped changes.
- Any future release must update `CHANGELOG.md` in the same release workflow so the repository history matches the published release notes.
- When cutting a semantic version release, keep the GitHub release notes and the `CHANGELOG.md` entry aligned.

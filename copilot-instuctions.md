# Copilot Instructions

Always use Conventional Commits for commit messages.

Format:

- `type(scope): subject`

Examples:

- `feat(cli): add dry-run option`
- `fix(manifest): handle missing modified time`
- `chore(deps): update requirements`
